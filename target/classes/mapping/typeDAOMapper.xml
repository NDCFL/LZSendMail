<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fz.dao.TypeDAO">
    <resultMap id="typeMap" type="typeVo">
        <id column="id" property="id"></id>
        <result column="typename" property="typename"></result>
        <result column="createTime" property="createTime"></result>
        <association property="userVo" javaType="userVo">
            <id column="userId" property="id"></id>
            <result column="loginAcc" property="loginAcc"></result>
        </association>
        <association property="agencyVo" javaType="agencyVo">
            <id column="agencyId" property="id"></id>
            <result column="leader" property="leader"></result>
        </association>
    </resultMap>
    <select id="queryById" resultMap="typeMap" parameterType="long">
        select t.*,u.id as userId,u.loginAcc,a.id as agencyId ,a.leader from t_type t,t_user u,t_agency a where  t.userId=u.id and t.agentyId=a.id and  t.id = #{id}
    </select>
    <delete id="delete" parameterType="long">
        delete from t_type where id = #{id}
    </delete>
    <select id="pageLists" resultMap="typeMap">
        select t.*,u.id as userId,u.loginAcc,a.id as agencyId ,a.leader from t_type t,t_user u,t_agency a where  t.userId=u.id and t.agentyId=a.id and t.userId=#{userId}  limit #{pageQuery.pageNo},#{pageQuery.pageSize}
    </select>
    <select id="listAll" resultMap="typeMap">
        select t.*,u.id as userId,u.loginAcc,a.id as agencyId ,a.leader from t_type t,t_user u,t_agency a where  t.userId=u.id and t.agentyId=a.id
    </select>
    <select id="count" resultType="int">
        select count(*) from t_type
    </select>
    <insert id="add" parameterType="typeVo">
        insert into t_type (typename, userId, agentyId) values ( #{typename}, #{userId}, #{agentyId})
    </insert>
    <update id="update">
        update t_type set typename=#{typename},userId=#{userId},agentyId=#{agentyId} where id = #{id}
    </update>
</mapper>